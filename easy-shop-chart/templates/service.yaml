apiVersion: v1
kind: Service
metadata:
  name: easy-shop-svc
  namespace: {{ .Values.namespace }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: easy-shop
  ports:
  - name: http
    protocol: TCP
    port: {{ .Values.service.port }}
    targetPort: 3000
